# Todoist Backup

Before using this script, remember to set the variable:

```bash
export TODOIST_API_TOKEN="your_token_here"
```

### Building the binary

To build the binary, run this script:

```
bun build ./backup.ts --compile --outfile todoist-backup
```
